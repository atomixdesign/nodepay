language: nodejs
node_js:
  - 12

branches:
  only:
    - master
    - next
    - /^v\d+\.\d+\.\d+$/

before_script:
  - yarn

script:
  - yarn build
  - yarn lint
  - yarn test

after_success:
  - yarn pack

before_deploy:
  - git config --global user.email ${GITHUB_EMAIL}
  - git config --global user.name ${GITHUB_USER}
  - git remote set-url origin "https://${GITHUB_TOKEN}@github.com/YOUR/PROJECT.git" > /dev/null 2>&1
  - git checkout release
  - echo "//registry.npmjs.org/:_authToken=\${NPM_TOKEN}" >> $HOME/.npmrc 2> /dev/null

deploy:
  - provider: releases
    api_key: $GITHUB_TOKEN
    file_glob: true
    file: "atomixdesign-nodepay-*.tgz"
    skip_cleanup: true
    on:
      tags: true
  - provider: script
    script: "yarn run lerna publish from-git"
    skip_cleanup: true
    on:
      branch: release
